@model IEnumerable<GestionRestaurant.Models.UTILISATEUR>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom_utilisateur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prenom_utilisateur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adresse_mail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mot_de_passe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_utilisateur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prenom_utilisateur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adresse_mail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mot_de_passe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_utilisateur }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_utilisateur }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_utilisateur })
        </td>
    </tr>
}

</table>
